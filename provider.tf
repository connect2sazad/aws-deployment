provider "aws" {
  region = var.region
  # are you looking for aws access key and secret key?
  # that is stored in aws credentials file
  # install aws cli and run aws configure to set up your credentials
}